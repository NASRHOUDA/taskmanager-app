apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: taskmanager
data:
  NODE_ENV: "production"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "taskmanager"
  DB_USER: "postgres"
  FRONTEND_URL: "http://localhost"
  API_URL: "http://localhost:5000"
