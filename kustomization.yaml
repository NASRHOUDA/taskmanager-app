apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kubernetes/

commonLabels:
  app.kubernetes.io/project: taskmanager
  app.kubernetes.io/managed-by: flux

commonAnnotations:
  kustomize.toolkit.fluxcd.io/force: "true"
