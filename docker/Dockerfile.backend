FROM node:18-alpine
WORKDIR /app
COPY backend/package*.json ./
RUN npm install --production
# Mettre à jour les paquets Alpine pour corriger les CVE OpenSSL
RUN apk update && apk upgrade --no-cache
COPY backend/ .
EXPOSE 5000
CMD ["node", "-r", "./tracing.js", "server.js"]
